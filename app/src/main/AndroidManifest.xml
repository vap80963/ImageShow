<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.tin.chigua.imageshow">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="false"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar"
        android:windowNoTitle="true">
        <activity
            android:name=".activity.AlbumsActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.AlbumItemActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".activity.SelectedImagesActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name=".GridImage"
            android:label="@string/app_name">
        </activity>
        <activity android:name=".activity.FullPictureActivity">
        </activity>
    </application>

</manifest>